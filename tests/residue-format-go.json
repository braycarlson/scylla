[
  {
    "fixture": "declarations.go",
    "category": "gofmt-import-grouping",
    "reason": "gofmt sorts an import block and moves a blank-line-separated group into place. Reordering an import moves tokens the source wrote, which the preservation relation of decision 10 forbids, so scylla re-indents the block and leaves the order alone."
  },
  {
    "fixture": "declarations.go",
    "category": "gofmt-declaration-context",
    "reason": "gofmt reads a declaration's shape from the tree and spaces a field name from its type accordingly, and pads the name and type columns of a struct with its tabwriter. The emitter reads the token stream, so it cannot tell a struct field's type from an index expression, and `src/format/align.rs` aligns the two runs decision 10 of the plan names — assignments and trailing comments — rather than every tabwriter cell."
  }
]
