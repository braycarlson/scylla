[
  {
    "fixture": "bat/src/bin/bat/clap_app.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "bat/src/bin/bat/config.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "bat/src/printer.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "bat/src/style.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "bat/src/theme.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/cli/src/decompress.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/cli/src/lib.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/cli/src/pattern.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/core/flags/config.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/core/flags/defs.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/core/flags/doc/version.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/globset/src/glob.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/ignore/src/dir.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/ignore/src/gitignore.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/ignore/src/types.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/ignore/src/walk.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/index/src/index.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/index/src/literal.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/matcher/src/lib.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/printer/src/util.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/regex/src/error.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "ripgrep/crates/searcher/src/searcher/mod.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/alloc/src/collections/btree/search.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/alloctests/tests/collections/binary_heap.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/alloctests/tests/str.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/builtins-test/benches/mem.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/builtins-test/tests/conv.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/compiler-builtins/src/mem/impls.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/crates/libm-macros/src/parse.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/libm/src/math/rem_pio2.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/libm/src/math/rem_pio2_large.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/compiler-builtins/libm/src/math/rem_pio2f.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/core/src/ops/control_flow.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/core/src/ops/range.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/core/src/slice/ascii.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/coretests/tests/ptr.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/portable-simd/crates/core_simd/examples/matrix_inversion.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/portable-simd/crates/core_simd/examples/spectral_norm.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/portable-simd/crates/core_simd/src/swizzle_dyn.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/proc_macro/src/bridge/client.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/proc_macro/src/bridge/fxhash.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/fs/tests.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/os/unix/net/ancillary.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/path.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/fs/mod.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/fs/unix.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/io/error/unix.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/io/kernel_copy/linux.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/pal/unix/mod.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/pal/unix/time.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/sys/process/unix/unix.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/std/src/thread/current.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/assert-instr-macro/src/lib.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/core_arch/src/x86/gfni.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-arm/src/context.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-arm/src/expression.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-arm/src/intrinsic.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-arm/src/matching.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-arm/src/typekinds.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-arm/src/wildcards.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/stdarch/crates/stdarch-gen-loongarch/src/main.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/addr2line-0.25.1/src/loader.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/addr2line-0.25.1/src/unit.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/cc-1.2.0/src/command_helpers.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/cc-1.2.0/src/lib.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/cc-1.2.0/src/tool.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/read/cfi.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/read/dwarf.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/read/loclists.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/read/op.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/read/unit.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/write/cfi.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/write/line.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/write/op.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/write/section.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/gimli-0.32.3/src/write/unit.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/moto-rt-0.16.0/src/fs.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/moto-rt-0.16.0/src/net.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/elf.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/endian.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/coff/import.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/elf/hash.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/elf/relocation.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/elf/section.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/elf/segment.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/macho/dyld_cache.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/macho/file.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/pe/resource.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/pe/rich.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/object-0.37.3/src/read/read_cache.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/rand-0.9.2/src/distr/uniform_int.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/rand-0.9.2/src/distr/weighted/weighted_index.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/rustc-demangle-0.1.27/src/legacy.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/unwinding-0.2.8/src/unwinder/arch/x86_64.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/wit-bindgen-0.57.1/src/rt/async_support/stream_support.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "rust/vendor/wit-bindgen-0.57.1/src/rt/mod.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "tigerbeetle/src/clients/rust/tests/tests.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "tigerbeetle/src/testing/vortex/rust_driver/src/main.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/bin/code/main.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/commands/agent_host.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/commands/serve_web.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/commands/tunnels.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/msgpack_rpc.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/state.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/tunnels/dev_tunnels.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/util/prereqs.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/util/tar.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  },
  {
    "fixture": "vscode/cli/src/util/zipper.rs",
    "category": "scip-shadowing",
    "reason": "The file declares the same name twice, an inner `let` over a parameter or over an outer `let`, and rust-analyzer answers a use inside the inner declaration's reach with the outer one. Its own index is inconsistent about it -- `bytes.try_reserve_exact(size)` reads the parameter and `bytes.resize(size, 0)` two lines below reads the local -- so the row measures the oracle's shadowing rather than scylla's, and every other name in the file agrees."
  }
]
