const one = <p>{value}</p>;
const call = <p>{compute(value)}</p>;
const sequence = <p>{(one, two)}</p>;
const spread = <p>{...items}</p>;
const empty = <p>{}</p>;
const comment = <p>{/* a note */}</p>;
const guarded = <p>{ready && <span>go</span>}</p>;
const mapped = <ul>{items.map((item) => <li key={item}>{item}</li>)}</ul>;
const ternary = <p>{ready ? <span>yes</span> : <span>no</span>}</p>;
const template = <p>{`a ${count} b`}</p>;
