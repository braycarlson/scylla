const sum = one + two * three - four / five % six;
const power = base ** exponent ** tail;
const negated = -value ** 2;
const compared = a < b && c >= d || e === f && g !== h;
const bitwise = a & b | c ^ d;
const shifted = a << b >> c >>> d;
const nullish = a ?? b;
const ternary = a ? b : c ? d : e;
const chained = target.field.other;
const indexed = target[key][other];
const called = handler(one, two)(three);
const optional = target?.field?.[key]?.(argument);
const unary = !flag;
const inverted = ~mask;
const typed = typeof value;
const voided = void 0;
const removed = delete target.field;
const grouped = (one, two, three);
const updated = counter++ + ++other;
const created = new Widget(one);
const bare = new Widget;
const membership = key in target;
const instance = value instanceof Widget;
sequence = one, two;
const decremented = counter-- - --other;
const loose = one == two;
const unequal = one != two;
const atMost = one <= two;
