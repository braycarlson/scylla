const held = value as string;
const twice = value as unknown as number;
const kept = value satisfies string;
const sure = value!;
const chained = holder!.name!.length;
const cast = <string>value;
const instantiated = identity<string>;
