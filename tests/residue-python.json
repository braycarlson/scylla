[
  {
    "fixture": "django/docs/lint.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "django/scripts/archive_eol_stable_branches.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "django/scripts/manage_translations.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "django/tests/backends/tests.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "django/tests/csrf_tests/tests.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "django/tests/logging_tests/tests.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/asyncio/base_events.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/ctypes/__init__.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/multiprocessing/connection.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/multiprocessing/resource_tracker.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/random.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/statistics.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "python/typing.py",
    "category": "fstring-self-documenting-spans",
    "reason": "The file writes an f-string self-documenting field -- `f\"... {params=}\"`. CPython folds the literal text in front of the field together with the echoed `params=` into one `Constant`, and gives that node the source span of both: the `Constant` ends after the `=` while the `FormattedValue` beside it began at the `{`, so the two siblings overlap by the whole width of the field's expression. A tree built from a linear stream of start and finish events cannot hold two siblings that interleave, so scylla writes the same text as the two `Constant` nodes the source actually spells. The values agree; only CPython's node count and its overlapping span do not, and no correction to the oracle can close that without inventing a different answer than CPython's."
  },
  {
    "fixture": "bat/tests/syntax-tests/highlighted/Python/battest.py",
    "category": "not-python",
    "reason": "The file is `bat`'s recorded highlighting output: every token is wrapped in ANSI colour escapes, so the bytes are a terminal transcript rather than a Python module. CPython's own `ast.parse` rejects it too, which is what makes it residue rather than a defect."
  },
  {
    "fixture": "vscode/extensions/copilot/test/simulation/fixtures/fixing/python/pyright_badtoken.py",
    "category": "not-python",
    "reason": "The file is a Copilot fixing fixture built to trigger tokenizer errors: it writes a U+00A0 non-breaking space between `import` and `sys`. CPython's own `ast.parse` rejects it with `invalid non-printable character U+00A0`, which is what makes it residue rather than a defect."
  },
  {
    "fixture": "vscode/extensions/vscode-colorize-tests/test/colorize-fixtures/test-freeze-56377.py",
    "category": "not-python",
    "reason": "The file is a colorizer freeze regression fixture: it writes a backslash continuation with a space behind it inside a `cmd = \"...\" \\ \"...\"` line. CPython's own `ast.parse` rejects it with `unexpected character after line continuation character`, which is what makes it residue rather than a defect."
  }
]
