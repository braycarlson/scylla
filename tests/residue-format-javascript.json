[
  {
    "fixture": "expressions.js",
    "category": "biome-line-breaking",
    "reason": "biome refuses the file: `-value ** 2` puts a unary operator immediately before an exponentiation, which does not parse, so the golden beside it is not the oracle's own output and the fixture is not comparable."
  },
  {
    "fixture": "control_flow.js",
    "category": "biome-line-breaking",
    "reason": "biome indents the body of a braceless `if` and deletes an empty statement. The emitter neither moves a line break nor deletes a token."
  },
  {
    "fixture": "declarations.js",
    "category": "biome-line-breaking",
    "reason": "biome explodes a multi-declarator `let` and a destructuring pattern across lines once they pass the width."
  },
  {
    "fixture": "objects.js",
    "category": "biome-literal-normalisation",
    "reason": "biome unquotes an object key that does not need quoting and normalises the quotes of one that does."
  },
  {
    "fixture": "jsx_attributes.js",
    "category": "biome-jsx-layout",
    "reason": "biome refuses the file: an empty expression container in an attribute position does not parse, so the golden beside it is not the oracle's own output and the fixture is not comparable."
  },
  {
    "fixture": "jsx_elements.js",
    "category": "biome-jsx-layout",
    "reason": "biome rebuilds a JSX element's own lines: an element breaks where it holds a child element, more than one expression container, or more than one attribute beside children, and its text is refilled. The emitter writes the element as the source spells it."
  },
  {
    "fixture": "jsx_expressions.js",
    "category": "biome-jsx-layout",
    "reason": "biome rebuilds a JSX element's own lines: an element breaks where it holds a child element, more than one expression container, or more than one attribute beside children, and its text is refilled. The emitter writes the element as the source spells it."
  },
  {
    "fixture": "jsx_fragments.js",
    "category": "biome-jsx-layout",
    "reason": "biome rebuilds a JSX element's own lines: an element breaks where it holds a child element, more than one expression container, or more than one attribute beside children, and its text is refilled. The emitter writes the element as the source spells it."
  },
  {
    "fixture": "jsx_positions.js",
    "category": "biome-jsx-layout",
    "reason": "biome rebuilds a JSX element's own lines: an element breaks where it holds a child element, more than one expression container, or more than one attribute beside children, and its text is refilled. The emitter writes the element as the source spells it."
  },
  {
    "fixture": "jsx_text.js",
    "category": "biome-jsx-layout",
    "reason": "biome rebuilds a JSX element's own lines: an element breaks where it holds a child element, more than one expression container, or more than one attribute beside children, and its text is refilled. The emitter writes the element as the source spells it."
  }
]
